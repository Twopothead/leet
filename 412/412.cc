
#include <stdlib.h>
#include <stdio.h> 
#include <string.h>
/**
 * Note: The returned array must be malloced, assume caller calls free().
 */
char ** fizzBuzz(int n, int* returnSize){
    char **words;
}
int main(){
    int num = 15;
    int returnSize = (int *)malloc(sizeof(int));
    fizzBuzz(num,returnSize);

    return 0;
}